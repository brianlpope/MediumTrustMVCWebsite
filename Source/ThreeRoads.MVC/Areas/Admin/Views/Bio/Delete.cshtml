@model ThreeRoads.Data.Models.Bio
           
<h2>Delete Confirmation</h2>

<p>Please confirm you want to delete the bio for @Html.Encode(Model.Name)</p>

@using (Html.BeginForm("Delete", "Bio", FormMethod.Post, new { id = Model.ID }))
{
    <input name="confirmButton" type="submit" value="Delete" />
}
